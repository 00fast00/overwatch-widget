---@meta defs

---@class ConfigFormatRule
---@field blueprint string Rules blueprint
---@field enabled boolean
---
---@field tags string[]? Tags for grouping/filtering.
---@field interval integer? Periodic interval
---@field ownTeam boolean? Run only with the current team instead per team?
---@field cooldown integer?
---@field category string?
---@field priority NotifyPriority?
---@field icon string?
---@field template string?
---@field channels string[]? Channels to force send the message (ignores priority on them).
---@field parameters table<string, any>? Custom parameters for the rule

---@class ConfigFormat
---@field rules table<string, ConfigFormatRule>?

-- The configuration settings for a rule
--
-- RuleConfig defines how a rule should behave, including notification
-- settings, cooldown periods, and custom parameters.
--
---@class RuleConfig
---@field category string Rule category for filtering
---@field priority NotifyPriority Alert priority level (default NotifyPriority.INFO)
---@field icon string Icon to display with the notifications
---@field template string Message template for notifications
--
---@field tags string[]? Tags for grouping/filtering.
---@field channels string[]? Channels to force send the message (ignores priority on them).
---@field ownTeam boolean? Run only with the current team instead per team?
---@field interval integer? Seconds between condition checks
---@field id string? Unique identifier
---@field enabled boolean? Whether the rule is enabled (default false)
---@field cooldown integer? Cooldown period after a trigger in seconds (default 0)
---@field parameters table<string, any>? Custom parameters for the rule

-- Notifications are generated by a rule when triggered.
--
---@class Notification
---@field team TeamContext
---@field config RuleConfig Rules config
--
---@field seconds number?
---@field channels string[]? Channels to force send the message (ignores priority on them).
---@field templateParams table<string, any>? Message template params
---@field message string? Templated message. Overrides templating/interpolation if given.
---@field priority NotifyPriority? Notification priority level. Overrides config if given.
---@field category string? Overrides config if given.
---@field icon string? Icon to display with the notification. Overrides config if given.
---@field parameters table<string, any>? Custom parameters for the

---@alias DispatchFun fun(n: Notification)
---@alias BpStartFun fun(game: GameContext, team: TeamContext, config: RuleConfig, dispatch: DispatchFun): function[]?
---@alias BpTriggerFun fun(game: GameContext, team: TeamContext, config: RuleConfig, state: RuleState, dispatch: DispatchFun): boolean

-- The logic of a rule
--
-- Rules define conditions and alerts for game events that should be monitored.
-- Each rule can be instantiated with specific configurations.
--
---@class Blueprint
--
---@field id string Unique identifier
---@field description string?
---@field defaultConfig RuleConfig
--
-- Start event-based monitoring.
--
---@field Start BpStartFun?
------@field start fun(refs: table, notify: fun(n: Notification)): function[]?
--
-- Trigger on intervals.
--
---@field Trigger BpTriggerFun?
--
--
-- NOTE(Fast): I've choosen the "@field" approach for functions as a workaround to make these "required" with lua-language-server.
--
---@see https://github.com/beyond-all-reason/recoil-lua-library/blob/main/library/generated/rts/Lua/LuaHandle.cpp.lua
---@class OverwatchUiCallins
--
-- Called whenever the window is resized.
--
---@field ViewResize fun(viewSizeX: number, viewSizeY: number)
--
-- Also available to LuaMenu.
--
---@field DrawScreen fun(viewSizeX: number, viewSizeY: number)
--
-- Called repeatedly when a key is pressed down.
--
-- Return true if you don't want other callins or the engine to also receive this keypress. A list of key codes can be seen at the SDL wiki.
--
---@field KeyPress fun(keyCode: number, mods: KeyModifiers, isRepeat: boolean, label: boolean, utf32char: number, scanCode: number, actionList: table): boolean
--
-- Called when the key is released.
--
---@field KeyRelease fun(keyCode: number, mods: KeyModifiers, label: boolean, utf32char: number, scanCode: number, actionList: table): boolean
--
-- Called when a mouse button is pressed.
--
-- The button parameter supports up to 7 buttons. Must return true for `MouseRelease` and other functions to be called.
--
---@field MousePress fun(x: number, y: number, button: number): boolean
--
-- Called when a mouse button is released.
--
-- Please note that in order to have Spring call `Spring.MouseRelease`, you need to have a `Spring.MousePress` call-in in the same addon that returns true.
--
---@field MouseRelease fun(x: number, y: number, button: number): boolean
--
-- Called when the mouse is moved.
--
---@field MouseMove fun(x: number, y: number, dx: number, dy: number, button: number)
--
-- Called when the mouse wheel is moved.
--
---@field MouseWheel fun(up: boolean, value: number)
--
--

---@class OverwatchUi: OverwatchUiCallins
---@field Init fun(logger: Logger?, game: GameContext?, config: Config?): boolean
---@field Shutdown fun(): boolean
-- Save saves the config, no questions asked.
---@field Save fun()
--
-- Set/update the given teamcontext.
---@field SetTeamContext fun(team: TeamContext)
-- Get a TeamContext by id.
---@field GetTeamContext fun(teamId: integer): TeamContext?
--
-- Informs the UI about specmode or allyteam changes.
---@field PlayerHasChanged fun()
---@field Log fun(n: Notification)
-- Renders a marquee notification.
---@field Marquee fun(n: Notification)

---@class ChannelControls
-- Returns the status if "state" is nil else sets the status to "state" and returns "state"
---@field Enabled fun(state: boolean?): boolean

---@class Channel
---@field id string
---@field Init fun(logger: Logger, game: GameContext, config: Config, ui: OverwatchUi): boolean
---@field Shutdown fun(): boolean
---@field GetControls fun(): ChannelControls
---@field GameFrame fun()
---@field Notify fun(n: Notification): boolean

return nil
